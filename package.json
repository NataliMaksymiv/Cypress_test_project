{
  "devDependencies": {
    "cypress": "^13.16.1",
    "mochawesome": "^7.1.3"
  },
  "scripts": {
    "test:config": "cypress run --env config-file=cypress.config.js",
    "test:config2": "cypress run --env config-file=cypress2.config.js"
  }
}
